
# 깃 연결


## 1️⃣ 기본 워크플로우
```bash
git clone <repo-url>
cd repository-name

git status
git pull origin main

git add .
git commit -m "메시지"
git push origin main
```
| 단계    | 명령어                    | 설명         |
| ----- | ---------------------- | ---------- |
| 클론    | `git clone <url>`      | 저장소 처음 받기  |
| 이동    | `cd repo`              | 폴더 진입      |
| 상태 확인 | `git status`           | 변경사항 확인    |
| 풀 | `git pull origin main`           | 깃헙 내려받기    |
| 스테이징  | `git add .`            | 전체 변경 추가   |
| 커밋    | `git commit -m "msg"`  | 로컬 저장      |
| 푸시    | `git push origin main` | GitHub 업로드 |




## 2️⃣ 브랜치 관리
```bash
git branch
git checkout -b new-feature

git branch -d new-feature
git push origin --delete new-feature

git checkout main
```
| 작업       | 명령어                      | 설명        |
| -------- | ------------------------ | --------- |
| 브랜치 목록   | `git branch`             | 현재 브랜치 확인 |
| 새 브랜치 생성 | `git checkout -b <name>` | 생성 + 이동   |
| 브랜치 삭제   | `git branch -d <name>`    | 로컬 내용 삭제        |
| 브랜치 삭제   | `git push origin --delete <name>`    | 깃헙 원격 삭제        |
| 브랜치 이동   | `git checkout <name>`    | 이동        |




## 3️⃣ 변경사항 되돌리기 / 복구
```bash
git restore filename
git restore .

git restore --staged filename
git reset filename

git reset --soft HEAD^
git revert <commit-id>

git log --oneline
git reflog

git merge --continue
```
| 상황      | 명령어                      | 설명       |
| ------- | ------------------------ | -------- |
| 파일 되돌리기 | `git restore filename`   | 특정 파일 만   |
| 수정 취소 | `git restore .` | 작업 폴더 전체 |
| 스태깅 취소   | `git restore --staged filename`                | 깃 추가 되돌리기 |
| 스태깅 제거  | `git reset filename`                | 깃 파일 유지 상태에서 스태깅 영역 제거 |
| 커밋 취소 | `git reset --soft HEAD^` | 커밋만 취소, 변경 내용은 유지 |
| 커밋 취소 | `git revert <commit-id>` | 되돌림 커밋 생성 히스토리 유지 |
| 커밋 로그 | `git log --oneline` | 커밋 기록 확인 |
| 실수 복구용 | `git reflog` | 삭제 커밋 포함 확인 |
| 병합 | `git merge --continue` | 파일 수정 후 합치기 |


## 4️⃣ 원격 저장소 업데이트 / 내려받기
```bash
## 작업 임시 저장
git stash
git stash pop

# upstream 연결 확인
git branch -vv

## 강제 동기화 
git fetch origin
git reset --hard origin/main
git clean -fd

# 확인 명령어
git status
git log --oneline --decorate -5
git ls-tree -r HEAD --name-only | grep <file>

```

- commit = 기록
- branch = 작업 단위
- push = 공유
- reset = 로컬 실수용
- revert = 배포된 코드 수정

