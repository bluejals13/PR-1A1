
# 깃 연결
---

## 1️⃣ 기본 워크플로우
```bash
git clone <repo-url>
cd repository-name

git status
git add .
git commit -m "메시지"
git push origin main
```
| 단계    | 명령어                    | 설명         |
| ----- | ---------------------- | ---------- |
| 클론    | `git clone <url>`      | 저장소 처음 받기  |
| 이동    | `cd repo`              | 폴더 진입      |
| 상태 확인 | `git status`           | 변경사항 확인    |
| 스테이징  | `git add .`            | 전체 변경 추가   |
| 커밋    | `git commit -m "msg"`  | 로컬 저장      |
| 푸시    | `git push origin main` | GitHub 업로드 |

---

## 2️⃣ 브랜치 관리
```bash
git branch
git checkout -b new-feature

git branch -d new-feature
git push origin --delete new-feature

git checkout main
```
| 작업       | 명령어                      | 설명        |
| -------- | ------------------------ | --------- |
| 브랜치 목록   | `git branch`             | 현재 브랜치 확인 |
| 새 브랜치 생성 | `git checkout -b <name>` | 생성 + 이동   |
| 브랜치 삭제   | `git branch -d <name>`    | 로컬 내용 삭제        |
| 브랜치 이동   | `git push origin --delete <name>`    | 깃헙 원격 삭제        |
| 브랜치 이동   | `git checkout <name>`    | 이동        |


---

## 3️⃣ 변경사항 되돌리기 / 복구
```bash
git checkout -- filename
git reset --hard HEAD^
git log

# ⚠️ 충돌 해결
git add <file>
git commit
```
| 상황      | 명령어                      | 설명       |
| ------- | ------------------------ | -------- |
| 파일 되돌리기 | `git checkout -- file`   | 수정 취소    |
| 커밋 되돌리기 | `git reset --hard HEAD^` | 최근 커밋 삭제 |
| 로그 확인   | `git log`                | 커밋 기록 보기 |

---

## 4️⃣ 원격 저장소 업데이트 / 내려받기
```bash
## 기본 업데이트
git pull origin main

## 강제 동기화 
git fetch origin
git reset --hard origin/main
git clean -fd

# 확인 명령어
git status
git log --oneline --decorate -5
git ls-tree -r HEAD --name-only | grep <file>

```
---
